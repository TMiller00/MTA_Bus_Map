{
  "name": "mta-bus-server",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "6.9.x"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client/ && npm install --only=dev && npm install && npm run build && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^4.0.0",
    "express": "^4.15.2",
    "request": "^2.81.0"
  }
}
